name: utilixa
description: "A high-performance premium global utility framework built with Flutter."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Localization & Real-time Date/Time Engine for Carbon Dashboard
  intl: ^0.20.2

  # Authenticator ecosystem modules
  google_sign_in: ^6.2.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Agar aap future me custom local fonts ya profile imagery components add karte hain:
  # assets:
  #   - assets/images/